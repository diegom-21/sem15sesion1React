import React from 'react';
import './Button.css'; // Asegúrate de crear este archivo para los estilos

const Button = () => {
    return (
        <div className='div-button'>
            <button className="see-all-button">
                See all
            </button>
        </div>

    );
}

export default Button;
